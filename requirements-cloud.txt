# Requirements ligero para deploy en la nube (Render/Railway/Fly.io)
# Sin torch, stable-baselines3, gymnasium, streamlit
# Versiones relajadas para que pip use wheels precompilados (sin compilar desde source)

# Dependencias principales (versiones relajadas para compatibilidad de wheels)
yfinance>=0.2.50
pandas>=2.0
numpy>=1.24,<2.0
ta>=0.11.0
plotly>=5.20
pyarrow>=15.0
scipy>=1.11
scikit-learn>=1.4

# API web
fastapi>=0.110
uvicorn[standard]>=0.30

# Broker y utilidades
alpaca-py>=0.17.0
python-dotenv>=1.0
schedule>=1.2
loguru>=0.7
requests>=2.31

# Machine Learning (solo XGBoost + VADER, sin torch)
xgboost>=2.0
vaderSentiment>=3.3

# Base de datos
sqlalchemy>=2.0

# Autenticación JWT
python-jose[cryptography]>=3.5

# Configuración
pydantic>=2.7
pydantic-settings>=2.4
pytest>=8.0
httpx>=0.27
