services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - ENVIRONMENT=development
    volumes:
      - ./logs:/app/logs
      - ./data/cache:/app/data/cache
    restart: unless-stopped
